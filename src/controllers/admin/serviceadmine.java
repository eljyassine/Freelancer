package controllers.admin;
import models.User;
import dbr.UserDb;

import java.sql.SQLException;
import java.util.List;
import java.util.stream.Stream;

public class serviceadmine {


/*
    public void freelancercount() throws SQLException {
        UserDb data=new UserDb();
        List<User> listP = data.selectAll();
        Stream<User> sp = listP.stream();
         int nbfreelancer =sp.filter(x -> x.getRole() ="Freelancer")
                .count();
    }

    public void clientcount() throws SQLException {
        UserDb data=new UserDb();
        List<User> listP = data.selectAll();
        Stream<User> sp = listP.stream();
        int nbfreelancer =sp.filter(x -> x.getRole() ="Client")
                .count();
    }*/
}
